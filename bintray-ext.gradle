import java.text.SimpleDateFormat

ext {

    bintray_user = project.properties.bintray_user
    bintray_api_key = project.properties.bintray_api_key
    bintray_name = module.name
    bintray_version_name = project.version
    bintray_version_desc = project.description
    bintray_version_released = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZZ", Locale.ENGLISH).format(new Date())
    bintray_version_vcsTag = project.version
    bintray_publications = ['AmetisteBintrayPublication']

}
